import type { Metadata, Viewport } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { AuthProvider } from "@/components/AuthProvider";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Freight Link Network",
  description: "South Africa's leading B2B logistics platform connecting transporters and suppliers.",
  icons: {
    icon: "/favicon.ico",
  },
};

// Explicit, deterministic viewport. We intentionally do NOT set
// maximumScale/userScalable — pinch-to-zoom stays enabled for accessibility.
// The iOS focus-zoom annoyance is fixed via 16px form controls in globals.css.
export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <AuthProvider>{children}</AuthProvider>
      </body>
    </html>
  );
}
